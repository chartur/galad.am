import { PublicSrcDirective } from "@directives/public-src.directive";

describe('PublicSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new PublicSrcDirective();
    expect(directive).toBeTruthy();
  });
});
