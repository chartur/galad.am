import { PropertyTranslatorPipe } from './property-translator.pipe';

describe('PropertyTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
