import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'am.galad.www',
  appName: 'galad.am',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
